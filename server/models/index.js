const AuthorModel = require('./author.model');
const BookModel = require('./author.model');
const UserModel = require('./user.model');
const ResetPassword = require('./passwordRest.model');

module.exports = {
    AuthorModel,
    BookModel,
    UserModel,
    ResetPassword,
};
